[package]
name = "bmi088"
version = "0.1.0"
edition = "2021"
description = "no_std driver for the BMI088 6-axis IMU (accelerometer + gyroscope)"

[dependencies]
embedded-hal = "1.0"

[dev-dependencies]
linux-embedded-hal = "0.4"
